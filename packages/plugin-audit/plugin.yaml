# 插件声明：id、版本、依赖与权限点（插件标准解剖结构）
id: dsh-plugin-audit
version: 1.0.0
label: 安全与审计
depends: ['dsh-plugin-platform-core', 'dsh-plugin-resource-core', 'dsh-plugin-iam']
provides:
  services:
    - audit
permissions:
  - audit.read
  - audit.rule.write
  - approval.read
  - approval.decide
