# 插件声明：id、版本、依赖与权限点（插件标准解剖结构）
id: dsh-plugin-modelgw
version: 1.1.0
label: 模型网关
depends: ['dsh-plugin-platform-core', 'dsh-plugin-usage']
provides:
  services:
    - modelGateway
permissions:
  - modelgw.read
  - modelgw.invoke
  - modelgw.admin
