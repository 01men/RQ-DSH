# 插件声明：id、版本、依赖与权限点（插件标准解剖结构）
id: dsh-plugin-dingtalk-bridge
version: 1.1.0
label: 钉钉桥接（群桥/出向投递/审批推送/告警通道）
depends: ['dsh-plugin-platform-core', 'dsh-plugin-iam', 'dsh-plugin-audit']
provides:
  services:
    - dingtalkBridge
permissions:
  - dingtalk.message.send
  - panel.read
