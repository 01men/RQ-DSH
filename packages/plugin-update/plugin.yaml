# 插件声明：id、版本、依赖与权限点（插件标准解剖结构）
id: dsh-plugin-update
version: 1.1.0
label: 平台自更新（版本检查/通知/一键升级）
depends: ['dsh-plugin-platform-core', 'dsh-plugin-iam']
provides:
  services:
    - update
permissions:
  - platform.update.read
  - platform.update.apply
