# OpenAPI 摘要（REST + 工具 + 服务键）—— CLI/Skill/Web 三端对齐的唯一事实源
plugin: dsh-plugin-nas
base: /api
endpoints:
  - GET/POST /api/nas · GET/PATCH /api/nas/:id · POST /api/nas/import（mcpServers JSON 一键纳管）
  - POST /api/nas/:id/transition|health|sync-tools
  - GET /api/nas/:id/fs[?path=] · GET /api/nas/:id/fs/info
  - GET /api/nas/:id/fs/file?path=&inline=（流式文件下载：浏览器拿到 bytes + content-disposition attachment|inline）
  - POST /api/nas/:id/fs/search|mkdir|rename|delete|upload|download|upload-many（经网关 fs_* 工具）
  - GET/PUT /api/skill-storage（Skill 包存储后端：local | 已纳管 NAS 资产）
  - POST /api/nas/authz/check（数据权限判定 {nasId,userId,paths[],op}）· GET /api/nas/authz/scope（作用域+矩阵快照）
  - GET/PUT /api/nas/authz/rules（PUT 携 ifVersion 乐观锁，冲突 409）· POST /api/nas/authz/rules/import（幂等种子）
  - POST /api/nas/authz/exceptions（资源级例外）· GET /api/nas/authz/decisions（判定留痕）
  - POST /api/nas/authz/share-requests（share 审批申请，审批人自动路由）· POST /api/nas/authz/reconcile（组织目录对账）
tools:
  - nas_list
  - nas_get
  - nas_health_check
  - nas_fs_list
  - nas_fs_search
  - nas_fs_mkdir
  - nas_fs_delete
  - nas_fs_upload
services:
  - key: nasRegistry
    access: ctx.nasRegistry
    description: NAS 资产注册/生命周期/探活 + MCP 文件网关客户端（synology-filestation 形态）+ Skill 包存储配置；全部文件操作进 usage 计量（nas:* 资源，calls/bytes 口径）
