node_modules/
data/
data-*/
boot.log
*.tmp

# 本地服务运行日志
service.log
dsh-run.log
.zcode/
.workbuddy/
cordis.local.yml

# 进程内探针残留
.tmp-probe-*
tests/.probe-*

# 部署 overlay（机器个性，勿版本化——见 docs/deploy-enterprise.md / deploy-mac-form-b.md）
overlay.yml
overlay-rq.yml

# npm 打包产物（发布时生成，不入库）
*.tgz
